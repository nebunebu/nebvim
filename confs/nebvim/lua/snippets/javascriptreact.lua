--[[
  React/JavaScript snippets

  Usage:
    These snippets are available in .jsx files (React with JavaScript)
    Uses the same React snippets as TypeScript but without type annotations

  Note:
    For TypeScript React (.tsx), use typescriptreact.lua instead
--]]

-- Simply use the TypeScript React snippets
-- They work fine for JavaScript React as well (just ignore the type annotations if desired)
return require("snippets.typescriptreact")
